# v

```
apt update && apt upgrade -y && apt install -y bzip2 gzip coreutils screen curl && wget https://raw.githubusercontent.com/foxxstoree12/install/main/setup.sh && chmod +x setup.sh && ./setup.sh
```
